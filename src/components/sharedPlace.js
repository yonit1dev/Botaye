import { DOM, MAP } from "../utils/globals";

class SharedPlace {
  constructor(coords, address) {
    this.location = {
      lat,
      lng,
    };
    this.address;
  }

  collectParams() {}

  renderSharedPlace() {}
}
