import { DOM, MAP } from "../utils/globals";

class SharedPlace {
  constructor(coords, address) {
    this.url = 
    this.coords = {
      lat,
      lng,
    };
    this.address;
  }

  collectParams() {

  }

  renderSharedPlace() {}
}
